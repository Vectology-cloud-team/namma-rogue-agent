# Agent

This directory will contain planner and executor code.

The planner may use local AI or NaMMA through provider interfaces. The deterministic executor turns plans into validated one-turn semantic actions.
